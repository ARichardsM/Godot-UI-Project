using Godot;		// https://docs.godotengine.org/en/stable/tutorials/scripting/c_sharp/c_sharp_basics.html#doc-c-sharp
using System;

public partial class Node2d : Node2D
{
    /*
    // Called when the node enters the scene tree for the first time.
    public override void _Ready()
    {
        Console.WriteLine("Hello World!");
        GD.Print("Hello from C# to Godot :)");
        var bitton = GetNode<Button>("Button");
        //bitton.ButtonPressed += ButtonPressed();
    }

    // Called every frame. 'delta' is the elapsed time since the previous frame.
    public override void _Process(double delta)
    {
    }

    public void ButtonPressed(int val)
    {
        GD.Print(Global.Data.num);
        Global.Data.num += 1;
        switch (val)
        {
            case 0:
                GD.Print("Left Button");
                break;

            case 1:
                GD.Print("Right Button");
                break;

        }

        GetTree().ChangeSceneToFile("MainMenu.tscn");
    }
    */
}
